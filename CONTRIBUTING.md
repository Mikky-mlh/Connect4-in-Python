# Contributing

Thanks for your interest in contributing to **Connect4-in-Python**! This document explains how to get started.

## Getting the code
1. Fork the repository on GitHub.
2. Clone your fork locally:
    git clone https://github.com/Mikky-mlh/Connect4-in-Python

## Development setup (Python)

We recommend using a **virtual environment** to manage the project's dependencies.

### Setting up the environment
1. Create a virtual environment (using `venv`):
    python3 -m venv .venv
2. Activate the virtual environment:
    # On macOS/Linux
    source .venv/bin/activate
    # On Windows (PowerShell)
    .venv\Scripts\Activate.ps1
   

## Running the game
You can run the game directly using the Python interpreter:
    python Connect4.py
    

## How to contribute
- Create a branch named: `feat/your-feature` or `fix/issue-number-description`
- Make small, focused commits with clear messages.
- Open a **Pull Request** against the `main` branch and reference the issue (e.g., "Closes #1").

## Coding style
- Keep code simple and readable.
- Follow **PEP 8**—the official style guide for Python code.

## Reporting issues and pull requests
- Use the issue templates provided in `.github/ISSUE_TEMPLATE/` when opening issues (if available).
- Fill the PR template when opening a pull request.

Thank you for contributing!
